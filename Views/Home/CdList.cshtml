<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css'>
    <title></title>
  </head>
  <body>
    <h1>List of Albums:</h1>
    <ul>
      @foreach(var Cd in Model)
      {
        <li><strong>@Cd.GetTitle()</strong></li>
        <br>
        <li><strong>Title:</strong> @Cd.GetTitle()</li>
        <li><strong>Artist:</strong> @Cd.GetArtist()</li>
        <li><strong>Released:</strong> @Cd.GetReleaseYear()</li>
      }
    </ul>
    <a href="/cds/new">Add a new album:</a>
  </body>
</html>
